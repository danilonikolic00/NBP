namespace MongoDB.Models
{
    public enum UserType
    {
        none,
        klijent,
        admin,
        prodavnica,
    }

    public enum Status
    {
        poruceno,
        dostavljeno,
    }
}
